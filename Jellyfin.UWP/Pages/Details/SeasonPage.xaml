<Page
    x:Class="Jellyfin.UWP.Pages.Details.SeasonPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:m="using:Jellyfin.Models"
    xmlns:uc="using:Jellyfin.UWP.Controls"
    xmlns:uwp="using:Jellyfin.ViewModels.Details"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <interactivity:Interaction.Behaviors>
        <interactivity:EventTriggerBehavior EventName="Loaded">
            <interactivity:InvokeCommandAction Command="{x:Bind ViewModel.LoadMediaInformationCommand}" CommandParameter="{x:Bind id}" />
        </interactivity:EventTriggerBehavior>
    </interactivity:Interaction.Behaviors>

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToObjectConverter x:Key="InverseBoolToVisibilityConverter" TrueValue="Collapsed" FalseValue="Visible" />
        <converters:BoolToObjectConverter x:Key="TrueRedFalseWhite" TrueValue="#cc3333" FalseValue="#ffffff" />
        <converters:BoolToObjectConverter x:Key="ClearHeartFilledHeart" TrueValue="&#xEB52;" FalseValue="&#xEB51;" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <uc:TopControl Grid.Row="0" />

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid
                Grid.ColumnSpan="2"
                Canvas.ZIndex="5"
                Background="Gray"
                Opacity=".8"
                Visibility="{x:Bind ViewModel.LoadMediaInformationCommand.IsRunning, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                <muxc:ProgressRing HorizontalAlignment="Center" VerticalAlignment="Center" VerticalContentAlignment="Center" IsActive="{x:Bind ViewModel.LoadMediaInformationCommand.IsRunning, Mode=OneWay}" />
            </Grid>

            <Image Source="{x:Bind ViewModel.ImageUrl, Mode=OneWay}" Grid.Column="0" />

            <ScrollViewer Grid.Column="1">
                <StackPanel>
                    <TextBlock Text="SEASON" Visibility="{x:Bind ViewModel.IsInDebug, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />

                    <StackPanel Height="115" Padding="30,0,30,0" Background="{StaticResource SystemControlHighlightHeaderBrush}">

                        <Grid Height="115">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{x:Bind ViewModel.MediaItem.SeriesName, Mode=OneWay}" FontSize="26" />
                                <TextBlock Text="{x:Bind ViewModel.MediaItem.Name, Mode=OneWay}" FontSize="17" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Right">
                                <Button Click="PlayClick" Margin="0,0,10,0" ToolTipService.ToolTip="Play">
                                    <Button.Background>
                                        <SolidColorBrush Color="#000000" Opacity="0" />
                                    </Button.Background>
                                    <SymbolIcon x:Name="play" Symbol="Play" />
                                </Button>

                                <Button Margin="10" ToolTipService.ToolTip="Shuffle">
                                    <Button.Background>
                                        <SolidColorBrush Color="#000000" Opacity="0" />
                                    </Button.Background>
                                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE8B1;" />
                                </Button>

                                <Button ToolTipService.ToolTip="Mark played" Margin="10" Command="{x:Bind ViewModel.PlayedStateCommand}">
                                    <Button.Background>
                                        <SolidColorBrush Color="#000000" Opacity="0" />
                                    </Button.Background>
                                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE73E;" Foreground="{x:Bind ViewModel.MediaItem.UserData.Played, Converter={StaticResource TrueRedFalseWhite}}" />
                                </Button>

                                <Button ToolTipService.ToolTip="Add to favorites" Margin="10" Command="{x:Bind ViewModel.FavoriteStateCommand}">
                                    <Button.Background>
                                        <SolidColorBrush Color="#000000" Opacity="0" />
                                    </Button.Background>
                                    <FontIcon FontFamily="Segoe MDL2 Assets" Foreground="{x:Bind ViewModel.MediaItem.UserData.IsFavorite, Mode=OneWay, Converter={StaticResource TrueRedFalseWhite}}" Glyph="{x:Bind ViewModel.MediaItem.UserData.IsFavorite, Mode=OneWay, Converter={StaticResource ClearHeartFilledHeart}}" />
                                </Button>
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <TextBlock Padding="30,10,30,10" Text="{x:Bind ViewModel.MediaItem.Overview, Mode=OneWay}" TextWrapping="WrapWholeWords" />

                    <StackPanel Padding="30,0,0,0">
                        <StackPanel>
                            <ListView
                                 x:Name="SeriesItems"
                                 ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                 ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                 ScrollViewer.HorizontalScrollMode="Enabled"
                                 ScrollViewer.VerticalScrollMode="Disabled"
                                 ItemsSource="{x:Bind ViewModel.SeriesMetadata, Mode=TwoWay}"
                                 IsItemClickEnabled="True"
                                 ItemClick="SeriesItems_ItemClick">
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="m:UIMediaListItemSeries">
                                        <StackPanel Orientation="Horizontal" Height="330">
                                            <Image Source="{x:Bind Url, Mode=OneTime}" MaxHeight="330" MaxWidth="500" VerticalAlignment="Center" Stretch="Fill" />
                                            <TextBlock Text="{x:Bind Name, Mode=OneTime}" FontSize="15" />

                                            <TextBlock Text="{x:Bind Description, Mode=OneTime}" Width="300" TextWrapping="WrapWholeWords" Opacity=".6" />

                                            <Button x:Name="EpisodePlay" Click="EpisodePlay_Click" Margin="0,5,0,0">
                                                <Button.Background>
                                                    <SolidColorBrush Color="#000000" Opacity="0" />
                                                </Button.Background>
                                                <SymbolIcon x:Name="play" Symbol="Play" />
                                            </Button>
                                            <Button ToolTipService.ToolTip="Mark played" Margin="10" Click="btn_EpisodeMarkPlayState_Click">
                                                <Button.Background>
                                                    <SolidColorBrush Color="#000000" Opacity="0" />
                                                </Button.Background>
                                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE73E;" Foreground="{x:Bind UserData.HasBeenWatched, Mode=TwoWay, Converter={StaticResource TrueRedFalseWhite}}" />
                                            </Button>
                                            <Button x:Name="btn_EpisodeMarkFavoriteState" ToolTipService.ToolTip="Add to favorites" Margin="10" Click="btn_EpisodeMarkFavoriteState_Click">
                                                <Button.Background>
                                                    <SolidColorBrush Color="#000000" Opacity="0" />
                                                </Button.Background>
                                                <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="{x:Bind UserData.IsFavorite, Converter={StaticResource ClearHeartFilledHeart}}" Foreground="{x:Bind UserData.IsFavorite, Converter={StaticResource TrueRedFalseWhite}}" />
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Grid>
</Page>
