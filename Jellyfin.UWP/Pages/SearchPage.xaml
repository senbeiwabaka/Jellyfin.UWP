<Page
    x:Class="Jellyfin.UWP.Pages.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:uc="using:Jellyfin.UWP.Controls"
    xmlns:m="using:Jellyfin.Models"
    xmlns:uwp="using:Jellyfin.UWP.ViewModels"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    NavigationCacheMode="Disabled">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToObjectConverter x:Key="InverseBoolToVisibilityConverter" TrueValue="Collapsed" FalseValue="Visible" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="75" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <uc:TopControl Grid.Row="0" PageType="{x:Bind PageType}" />

        <AutoSuggestBox
            x:Name="asbSearch"
            PlaceholderText="Search"
            QueryIcon="Find"
            Grid.Row="1"
            TabIndex="0">
            <interactivity:Interaction.Behaviors>
                <interactivity:EventTriggerBehavior EventName="TextChanged">
                    <interactivity:InvokeCommandAction Command="{x:Bind ViewModel.LoadSearchCommand}" CommandParameter="{x:Bind asbSearch.Text, Mode=OneWay}" />
                </interactivity:EventTriggerBehavior>
                <interactivity:EventTriggerBehavior EventName="QuerySubmitted">
                    <interactivity:InvokeCommandAction Command="{x:Bind ViewModel.LoadSearchCommand}" CommandParameter="{x:Bind asbSearch.Text, Mode=OneWay}" />
                </interactivity:EventTriggerBehavior>
            </interactivity:Interaction.Behaviors>
        </AutoSuggestBox>

        <ScrollViewer Grid.Row="2">
            <StackPanel Margin="10,5">
                <StackPanel Visibility="{x:Bind ViewModel.LoadSearchCommand.IsRunning, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <muxc:ProgressRing HorizontalAlignment="Center" VerticalAlignment="Center" VerticalContentAlignment="Center" IsActive="{x:Bind ViewModel.LoadSearchCommand.IsRunning, Mode=OneWay}" />
                </StackPanel>

                <StackPanel Visibility="{x:Bind ViewModel.LoadSearchCommand.IsRunning, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <StackPanel Visibility="{x:Bind ViewModel.HasMoviesResult, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Movies" FontSize="22" FontWeight="Bold" />

                        <ListView
                        ScrollViewer.VerticalScrollBarVisibility="Disabled"
                        ScrollViewer.HorizontalScrollBarVisibility="Visible"
                        ScrollViewer.HorizontalScrollMode="Enabled"
                        ScrollViewer.VerticalScrollMode="Disabled"
                        ItemClick="ListView_ItemClick"
                        ItemsSource="{x:Bind ViewModel.MovieMediaList, Mode=OneWay}"
                        IsItemClickEnabled="True">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Background="Transparent" Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="m:UIMediaListItem">
                                    <StackPanel Width="220" Margin="0,10">
                                        <StackPanel PointerEntered="StackPanel_PointerEntered" PointerExited="StackPanel_PointerExited" Height="330">
                                            <uc:ViewedIconControl PositionLeft="195" PositionTop="5" HasBeenViewed="{x:Bind UserData.HasBeenWatched, Mode=OneWay}" />
                                            <uc:PlayButtonControl PositionLeft="90" PositionTop="142" />
                                            <uc:ViewedFavoriteButtonControl PositionLeft="120" PositionTop="290" Item="{x:Bind}" ButtonClick="ViewedFavoriteButtonControl_ButtonClick" />

                                            <Image Source="{x:Bind Url}" HorizontalAlignment="Center" Canvas.ZIndex="5" Stretch="Fill" Height="330" />

                                            <Canvas Visibility="Collapsed">
                                                <StackPanel Height="330" Width="220" Canvas.Top="-330" Background="#B3646464" Canvas.ZIndex="1"></StackPanel>
                                            </Canvas>
                                        </StackPanel>

                                        <TextBlock Text="{x:Bind Name}" FontSize="14" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>

                    <StackPanel Visibility="{x:Bind ViewModel.HasSeriesResult, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Shows" FontSize="22" FontWeight="Bold" />

                        <ListView
                            ScrollViewer.VerticalScrollBarVisibility="Disabled"
                            ScrollViewer.HorizontalScrollBarVisibility="Visible"
                            ScrollViewer.HorizontalScrollMode="Enabled"
                            ScrollViewer.VerticalScrollMode="Disabled"
                            ItemClick="ListView_ItemClick"
                            ItemsSource="{x:Bind ViewModel.SeriesMediaList, Mode=OneWay}"
                            IsItemClickEnabled="True">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Background="Transparent" Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="m:UIMediaListItemSeries">
                                    <StackPanel Width="220" Margin="0,10">
                                        <StackPanel PointerEntered="StackPanel_PointerEntered" PointerExited="StackPanel_PointerExited" Height="330">
                                            <uc:ViewedIconControl PositionLeft="195" PositionTop="5" HasBeenViewed="{x:Bind UserData.HasBeenWatched, Mode=OneWay}" />
                                            <uc:CountControl PositionLeft="190" PositionTop="10" UserData="{x:Bind UserData, Mode=OneWay}" />
                                            <uc:PlayButtonControl PositionLeft="90" PositionTop="142" />
                                            <uc:ViewedFavoriteButtonControl PositionLeft="120" PositionTop="290" Item="{x:Bind}" ButtonClick="ViewedFavoriteButtonControl_ButtonClick" />

                                            <Image Source="{x:Bind Url, Mode=OneWay}" HorizontalAlignment="Center" Canvas.ZIndex="5" Stretch="Fill" Height="330" />

                                            <Canvas Visibility="Collapsed">
                                                <StackPanel Height="330" Width="220" Canvas.Top="-330" Background="#B3646464" Canvas.ZIndex="1"></StackPanel>
                                            </Canvas>
                                        </StackPanel>

                                        <TextBlock Text="{x:Bind Name, Mode=OneWay}" FontSize="14" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>

                    <StackPanel Visibility="{x:Bind ViewModel.HasEpisodesResult, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                        <TextBlock Text="Episodes" FontSize="22" FontWeight="Bold" />

                        <ListView
                            ScrollViewer.VerticalScrollBarVisibility="Disabled"
                            ScrollViewer.HorizontalScrollBarVisibility="Visible"
                            ScrollViewer.HorizontalScrollMode="Enabled"
                            ScrollViewer.VerticalScrollMode="Disabled"
                            ItemClick="ListView_ItemClick"
                            ItemsSource="{x:Bind ViewModel.EpisodesMediaList, Mode=OneWay}"
                            IsItemClickEnabled="True">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Background="Transparent" Orientation="Horizontal" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="m:UIMediaListItem">
                                    <StackPanel Width="380" Margin="0,10">
                                        <StackPanel PointerEntered="StackPanel_PointerEntered" PointerExited="StackPanel_PointerExited" Height="214">
                                            <uc:ViewedIconControl PositionLeft="350" PositionTop="5" HasBeenViewed="{x:Bind UserData.HasBeenWatched, Mode=OneWay}" />
                                            <uc:PlayButtonControl PositionLeft="175" PositionTop="95" />
                                            <uc:ViewedFavoriteButtonControl PositionLeft="280" PositionTop="180" Item="{x:Bind}" ButtonClick="ViewedFavoriteButtonControl_ButtonClick" />

                                            <Image Source="{x:Bind Url}" Height="214" HorizontalAlignment="Center" Canvas.ZIndex="5" Stretch="Fill" />

                                            <Canvas Visibility="Collapsed">
                                                <StackPanel Height="214" Width="380" Canvas.Top="-214" Background="#B3646464" Canvas.ZIndex="1"></StackPanel>
                                            </Canvas>
                                        </StackPanel>

                                        <TextBlock Text="{x:Bind Name}" FontSize="14" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
