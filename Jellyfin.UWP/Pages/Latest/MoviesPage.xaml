<Page
    x:Class="Jellyfin.UWP.Pages.Latest.MoviesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:uc="using:Jellyfin.UWP.Controls"
    xmlns:m="using:Jellyfin.Models"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <converters:BoolToObjectConverter x:Key="InverseBoolToVisibilityConverter" TrueValue="Collapsed" FalseValue="Visible" />

        <DataTemplate x:Key="UIContinueMediaListItemDataTemplate" x:DataType="m:UIMediaListItem">
            <StackPanel Width="380">
                <StackPanel PointerEntered="StackPanel_PointerEntered" PointerExited="StackPanel_PointerExited" Height="214">
                    <uc:ViewedIconControl PositionLeft="350" PositionTop="10" UserItem="{x:Bind Mode=OneWay}" />
                    <uc:PlayButtonControl PositionLeft="175" PositionTop="95" />
                    <uc:ViewedFavoriteButtonControl PositionLeft="285" PositionTop="180" UserItem="{x:Bind}" />

                    <Image Source="{x:Bind Url}" Height="214" HorizontalAlignment="Center" Canvas.ZIndex="5" />

                    <Canvas Visibility="Collapsed">
                        <StackPanel Height="214" Width="380" Canvas.Top="-214" Background="#B3646464" Canvas.ZIndex="1"></StackPanel>
                    </Canvas>
                </StackPanel>

                <TextBlock Text="{x:Bind Name}" FontSize="14" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="UIMediaListItemDataTemplate" x:DataType="m:UIMediaListItem">
            <StackPanel Width="220">
                <StackPanel PointerEntered="StackPanel_PointerEntered" PointerExited="StackPanel_PointerExited" Height="330">
                    <uc:ViewedIconControl PositionLeft="195" PositionTop="10" UserItem="{x:Bind Mode=OneWay}" />
                    <uc:PlayButtonControl PositionLeft="95" PositionTop="150" />
                    <uc:ViewedFavoriteButtonControl PositionLeft="120" PositionTop="295" UserItem="{x:Bind}" />

                    <Image Source="{x:Bind Url}" Height="330" HorizontalAlignment="Center" Canvas.ZIndex="5" Stretch="Fill" />

                    <Canvas Visibility="Collapsed">
                        <StackPanel Height="330" Width="220" Canvas.Top="-330" Background="#B3646464" Canvas.ZIndex="1"></StackPanel>
                    </Canvas>
                </StackPanel>

                <TextBlock Text="{x:Bind Name}" FontSize="14" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" />
            </StackPanel>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <uc:TopControl Grid.Row="0" />

        <ScrollViewer Grid.Row="1" Margin="10" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <StackPanel Margin="0,0,0,10"  Visibility="{x:Bind ViewModel.HasResumeMedia, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="Continue Watching" FontSize="40" />

                    <ListView
                        x:Name="lv_Continue"
                        ItemsSource="{x:Bind ViewModel.ResumeMediaList, Mode=OneWay}"
                        ItemClick="{x:Bind MediaClickItemList}"
                        IsItemClickEnabled="True"
                        ItemTemplate="{StaticResource UIContinueMediaListItemDataTemplate}">
                        <ListView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsStackPanel Background="Transparent" Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </ListView.ItemsPanel>
                    </ListView>
                </StackPanel>

                <StackPanel Margin="0,0,0,10" Grid.Row="1">
                    <TextBlock Text="Recently Added Movies" FontSize="40" />

                    <GridView
                        x:Name="lv_Latest"
                        ItemsSource="{x:Bind ViewModel.LatestMediaList, Mode=OneWay}"
                        ItemClick="{x:Bind MediaClickItemList}"
                        IsItemClickEnabled="True"
                        ItemTemplate="{StaticResource UIMediaListItemDataTemplate}">
                        <GridView.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ItemsWrapGrid Background="Transparent" Orientation="Horizontal" />
                            </ItemsPanelTemplate>
                        </GridView.ItemsPanel>
                    </GridView>
                </StackPanel>

                <StackPanel Margin="0,0,0,10" x:Name="sp_Recommendations" Visibility="{x:Bind ViewModel.HasRecommendationMedia, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}">
                </StackPanel>

                <StackPanel Margin="0,0,0,10" Visibility="{x:Bind ViewModel.HasRecommendationMedia, Mode=OneWay, Converter={StaticResource InverseBoolToVisibilityConverter}}">
                    <TextBlock Text="No movie suggestions are currently available. Start watching and rating your movies, and then come back to view your recommendations." />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
