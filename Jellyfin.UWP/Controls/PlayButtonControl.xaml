<UserControl
    x:Class="Jellyfin.UWP.Controls.PlayButtonControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    Canvas.ZIndex="2"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Canvas Background="Transparent" Visibility="{x:Bind isPlaybackEnabled, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" >
        <Button x:Name="MediaPlayerButton" Click="MediaPlayButton_Click" Background="Black" Canvas.Left="{x:Bind PositionLeft}" Canvas.Top="{x:Bind PositionTop}">
            <SymbolIcon Symbol="Play" />
        </Button>
    </Canvas>
</UserControl>
